class Singer 
{
	private $name; 
	private $age; 

	private addSub ( $idUser)
	{
		// them nguoi dung` theo doi~ ca sy~
		...
	}
	private addSong ($song)
	{
		foreach($UserSub as $item)
		{
			$item->notice($this->name);
		}
	}

	get();
	set();
}


class UserSub 
{
	private $name;
	private $user;
	private $pass;
	private notice ($singerName)
	{
		echo "a new song from ".$singerName;
}